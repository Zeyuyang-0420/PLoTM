# Copy to .env . Read server-side by server.py — the key never reaches the browser.
ANTHROPIC_API_KEY=

# optional
# PROGLOF_MODEL=claude-opus-4-8
# PROGLOF_ROOT=/mnt/scratch/ZY2/Hackathon        # where pipeline_results/ lives (build step only)
# PROGLOF_DEMO_DATA=./data/demo_data.json        # serve a prebuilt data file
